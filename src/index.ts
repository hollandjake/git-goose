export { GitGlobalConfig, Patchers, type ContextualGitConfig } from './config';
export { committable, git as default, git } from './plugin';
export type { Commit, CommittableModel, Diff } from './types';
